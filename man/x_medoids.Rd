% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unpackaged_utils.R
\name{x_medoids}
\alias{x_medoids}
\title{k-medoids across a range, returning all internal cluster-quality measures}
\usage{
x_medoids(
  data,
  min_clusters = 2,
  max_clusters = 10,
  metric = "euclidean",
  sort_by = c("silhouette", "dissimilarity", "isolation", "diameter", "separation"),
  ...
)
}
\arguments{
\item{data}{A data frame / tibble of numeric features.}

\item{min_clusters}{Smallest k to try (≥ 2 – silhouette is undefined for k = 1).}

\item{max_clusters}{Largest  k to try.}

\item{metric}{"euclidean", "manhattan", … as accepted by pam().}

\item{sort_by}{Which measure to sort on.
Choices: "silhouette" (default),
"dissimilarity",
"isolation", "diameter",
"separation".}

\item{...}{Extra arguments forwarded to cluster::pam().}
}
\value{
A tibble with one row per k, containing:
• the scalar measures (columns above)
• \code{model}   – the pam object
• \code{data}    – original data + \code{.cluster} factor
• \code{clusinfo} and \code{silhouette_widths} for full drill-down
}
\description{
k-medoids across a range, returning all internal cluster-quality measures
}
